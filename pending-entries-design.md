# 未処理伝票・売掛連動 設計メモ

作成日: 2026-07-10

## 目的

赤伝、訂正、追加請求、先行受注を忘れずに次回 invoice へ反映し、将来の売掛管理までつなげる。

現場作業データや売上確定済みデータを直接書き換えず、未反映の伝票として別管理する。

## 対象業務

- 赤伝
- 訂正
- 追加請求
- 値引き
- 送料調整
- 先行受注

先行受注は、発注後いつ商品が届くか未定のケースがあるため、出荷日必須にはしない。

## 管理単位

基本単位:

- 輸入社コード
- 得意先コードまたは得意先名
- 伝票単位

売掛残・集計の主軸:

- 輸入社別

登録単位:

- 得意先単位

## 状態

`pending`

- 未反映
- invoice 作成時に候補として表示する

`applied`

- invoice へ反映済み
- 反映先 invoice 番号、反映日時を保持する

`hold`

- 保留
- invoice 反映候補には出すが、明示的に保留とわかるように表示する

`cancelled`

- 取消
- 履歴として残す
- invoice 反映候補には出さない

## 伝票種別

`advance_order`

- 先行受注
- 商品到着日未定でも登録可能
- 本受注または invoice 反映時に `applied` にする

`credit_note`

- 赤伝
- 金額は原則マイナス

`correction`

- 訂正
- プラス・マイナスどちらもあり得る

`extra_charge`

- 追加請求
- 金額は原則プラス

`discount`

- 値引き
- 金額は原則マイナス

`shipping_adjustment`

- 送料調整
- 輸入社別送料や前回送料差額に使う

## 画面設計

### 発注入力側

追加タブ:

- 未処理伝票

機能:

- 新規登録
- 一覧表示
- 輸入社コードで絞り込み
- 得意先で絞り込み
- 状態で絞り込み
- 登録済み内容の編集
- 取消

初期表示:

- `pending`
- `hold`

登録フォーム:

- 種別
- 輸入社コード
- 輸入社名
- 得意先コード
- 得意先名
- 商品コード
- 商品名または内容
- 数量
- 単位
- 単価
- 金額
- 理由・メモ
- 予定日 任意
- 状態

### invoice 出力側

invoice 作成時に、対象輸入社の未反映伝票を表示する。

表示条件:

- `status in ('pending', 'hold')`
- invoice 対象の輸入社コードと一致
- 取消済みは除外

確認方法:

- チェックボックスで反映対象を選択
- YES を押した伝票だけ Excel invoice 明細へ自動追加
- 反映後に `applied` へ更新

表示例:

```text
未反映データがあります

輸入社: FBI

[ ] Sushi Samba / 赤伝 / 前回請求分 冷凍赤身 / -5,000
[ ] Sushi Samba / 先行受注 / 大葉 10パック / 2,000
```

Excel 明細反映例:

```text
Adjustment / Credit from previous invoice   -5,000
Advance order / 大葉 10パック                2,000
```

## 売掛管理との接続

将来の売掛残は以下で計算する。

```text
売掛残 = invoice 合計 + 追加請求 - 赤伝 - 値引き - 入金
```

実装上は、invoice に反映された未処理伝票を invoice 明細として扱う。

売掛管理に必要な将来テーブル:

- `invoices`
- `invoice_lines`
- `payments`

今回の `pending_entries` は、`invoice_lines` に入る前の未処理キューとして使う。

## 実装順

1. `pending_entries` SQL 作成
2. 発注入力側に未処理伝票タブを追加
3. 未処理伝票の登録・編集・取消を実装
4. invoice 出力前に未反映候補を表示
5. YES 選択分を Excel invoice 明細へ追加
6. 反映済みに更新
7. 売掛管理テーブルを追加
8. 売掛残・入金消込へ拡張

## 注意点

- 売上確定済みの `sales_records` は直接修正しない
- 赤伝や訂正は履歴として残す
- 自動反映ではなく、最初は確認制にする
- 反映済み伝票は原則編集不可にする
- 修正が必要な場合は取消または逆伝票で対応する

